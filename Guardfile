# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'rspec', :version => 2 do
  watch(%r{^lib/})                    { "spec" }
  watch(%r{^spec/controllers/})       { "spec" }
  watch(%r{^spec/dummy/app/})         { "spec" }
  watch(%r{^spec/dummy/controllers/}) { "spec" }
  watch(%r{^spec/straides/})          { "spec" }
end

